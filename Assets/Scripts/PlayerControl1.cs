using UnityEngine;

public class PlayerControl1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Это Карево А.С. и это мой любимый куб на Цитадели");//Данная команда сработает перед первым кадром игры 1 раз
        //Команда позволяет вывести сообщение в консоль
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
